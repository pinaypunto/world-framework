body > article

  transform-origin: 100% 100%
  vendor(animation-duration, ROUTER_ANIMATION_DURATION)
  vendor(animation-timing-function, ROUTER_ANIMATION)
  vendor(transform, translate3d(0,0,0))

  &.active

    // Animation SLIDE
    &[data-animation="slide"]
      &[data-direction="in"]
        vendor(animation-name, slide_in)
      &[data-direction="out"]
        vendor(animation-name, slide_out)
      &[data-direction="back-in"]
        vendor(animation-name, slide_back_in)
      &[data-direction="back-out"]
        vendor(animation-name, slide_back_out)

    // Animation COVER
    &[data-animation="cover"]
      &[data-direction="in"]
        vendor(animation-name, cover_in)
      &[data-direction="out"]
        vendor(animation-name, cover_out)
      &[data-direction="back-in"]
        vendor(animation-name, cover_back_in)
      &[data-direction="back-out"]
        vendor(animation-name, cover_back_out)

    // Animation ROOM
    &[data-animation="room"]
      &[data-direction="in"]
        vendor(animation-name, room_in)
      &[data-direction="out"]
        vendor(animation-name, room_out)
      &[data-direction="back-in"]
        vendor(animation-name, room_back_in)
      &[data-direction="back-out"]
        vendor(animation-name, room_back_out)

    // Animation CUBE
    &[data-animation="cube"]
      &[data-direction="in"]
        vendor(animation-name, cube_in)
      &[data-direction="out"]
        vendor(animation-name, cube_out)
      &[data-direction="back-in"]
        vendor(animation-name, cube_back_in)
      &[data-direction="back-out"]
        vendor(animation-name, cube_back_out)


// Keyframes SLIDE
@keyframes slide_in
  0%
    vendor(transform, translateX(100%))
  100%
    vendor(transform, translateX(0))

@keyframes slide_out
  0%
    vendor(transform, translateX(0))
  100%
    vendor(transform, translateX(-100%))

@keyframes slide_back_in
  0%
    vendor(transform, translateX(-100%))
  100%
    vendor(transform, translateX(0))

@keyframes slide_back_out
  0%
    vendor(transform, translateX(0))
  100%
    vendor(transform, translateX(100%))

// Keyframes COVER
@keyframes cover_in
  0%
    vendor(transform, translateY(100%))
  100%
    vendor(transform, translateY(0))

@keyframes cover_out
  0%
    vendor(transform, translateY(0))
  100%
    opacity: 0.3
    vendor(transform, translateY(0) translateZ(-10px) rotateX(5deg))

@keyframes cover_back_in
  0%
    opacity: 0.3
    vendor(transform, translateY(0) translateZ(-10px) rotateX(5deg))
  100%
    vendor(transform, translateY(0))

@keyframes cover_back_out
  0%
    vendor(transform, translateY(0))
  100%
    vendor(transform, translateY(100%))

// Keyframes ROOM
@keyframes room_in
  0%
    vendor(transform, translateX(100%))
  100%
    vendor(transform, translateX(0))

@keyframes room_out
  0%
    vendor(transform, translateX(0))
  100%
    vendor(transform, translateX(-100%))

@keyframes room_back_in
  0%
    vendor(transform, translateX(-100%))
  100%
    vendor(transform, translateX(0))

@keyframes room_back_out
  0%
    vendor(transform, translateX(0))
  100%
    vendor(transform, translateX(100%))

// Keyframes CUBE
@keyframes cube_in
  0%
    vendor(transform, translateX(100%))
  100%
    vendor(transform, translateX(0))

@keyframes cube_out
  0%
    vendor(transform, translateX(0))
  100%
    vendor(transform, translateX(-100%))

@keyframes cube_back_in
  0%
    vendor(transform, translateX(-100%))
  100%
    vendor(transform, translateX(0))

@keyframes cube_back_out
  0%
    vendor(transform, translateX(0))
  100%
    vendor(transform, translateX(100%))


